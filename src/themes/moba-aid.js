import { createTheme } from 'themes/createTheme'

export default (assets) => {
  return createTheme({
    name: 'Moba AID',
    colors: {
      primary: '#1A1FDF',
      secondary: '#F79900'
    }
  })
}
